server {
    listen 80;
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Redirigir las peticiones de la API al backend de PHP
    location /controlador/ {
        proxy_pass http://backend:80;
    }
}